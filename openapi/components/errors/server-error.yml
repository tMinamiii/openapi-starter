Error:
  description: サーバサイドエラー.
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            default: ""
        example:
          message: "Server error."

